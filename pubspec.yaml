name: shopping_cart
description: A new Flutter application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.0
  equatable: ^2.0.3
  firebase_core: ^1.6.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  lints: ^1.0.1
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
